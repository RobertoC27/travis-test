language: python
python:
  - "2.6"
  - "2.7"
# command to install dependencies
install:
  - pip install unittest2
# command to run tests
script:
  - test.py # or py.test for Python versions 3.5 and below
